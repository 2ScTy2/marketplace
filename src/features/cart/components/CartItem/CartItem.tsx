import { observer } from "mobx-react-lite";

import * as S from './CardItem.styled'

export default observer(function CartItem() {
    return (
        <S.CartWrapper>
            asasa
        </S.CartWrapper>
    )
})